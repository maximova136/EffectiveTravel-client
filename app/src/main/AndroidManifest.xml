<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.et">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".gui.MenuActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".gui.LoginActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".gui.TransportStatsActivity" />
        <activity android:name=".gui.SignupActivity"></activity>
        <activity android:name=".gui.TestActivity"></activity>
        <activity android:name=".gui.StationsListActivity"></activity>
        <activity android:name=".gui.RoutesListActivity"></activity>
    </application>

</manifest>